name: OpenSSF

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  id-token: write
  contents: read

jobs:
  call:
    uses: opiproject/actions/.github/workflows/scorecard.yml@main
    secrets: inherit
